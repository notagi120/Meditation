<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Meditation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item> <!-- 文字色の設定 -->
        <item name="android:fontFamily">casual</item> <!-- フォントの設定 -->
    </style>

    <style name="Theme.Meditation" parent="Base.Theme.Meditation" />
</resources>